# Feature State — add-provider-discovery-and-api-usage

- **change:** `add-provider-discovery-and-api-usage`
- **scope_id:** `provider-discovery-api-usage-main`
- **attempt_id:** `implementation-1`
- **stage:** `accepted`
- **result:** The versioned main provider/usage and Day 8 diagnostic implementation is source-frozen for independent verification. It includes 30 API-key profiles, bundled and remote discovery, native text streaming, scoped key settings, searchable large catalog picker, API-only usage, safe provider errors, and an uncompacted short/long/overflow demo. OpenCode Zen/Go were removed from enabled profiles after verifying Pi's mixed per-model routing; they remain documented for later protocol work.
- **initial_tier / current_tier:** `T2 / T2`
- **risk evidence:** New provider protocols, API keys, catalog/selection lifecycle, and public usage/error semantics span core, transport, secure storage, session restore, and UI.
- **base_revision:** `fa483e07685307cccd10ca229f80c107485eb591`
- **starting_dirty_diff:** No tracked changes; the new OpenSpec change directory was untracked at assignment. No other worktree changes were present.
- **contract_revision:** `planning-1`; `openspec validate add-provider-discovery-and-api-usage --strict` exited 0 and `openspec status` reported 4/4 artifacts complete.
- **recommended_mode / execution_mode:** `heavy / heavy`
- **rationale:** Coupled protocol, credential, refresh, restore, and presentation contracts need one writer and independent AC verification on a frozen result.
- **selection_source:** Root orchestrator assigned this agent as the sole Sol writer under the user's explicit full-implementation instruction.
- **model_tiers_used:** Sol writer active. Repository policy names an independent DeepSeek verifier; that model is not available in the built-in collaboration tool set for this run. No DeepSeek verification is claimed.
- **expected_changed_paths:** `lib/core/llm/**`, `lib/core/agents/**` for selection/error compatibility, `lib/infrastructure/llm/**`, `lib/infrastructure/credentials/**`, `lib/features/settings/**`, `lib/features/chat/**`, `lib/app.dart`, `assets/models_fallback.json`, `pubspec.yaml`, `lib/day08_main.dart`, `lib/demos/**`, `tool/**`, corresponding `test/**`, this state file, and assigned OpenSpec tasks.
- **excluded scope:** Commit/push/archive until final gate, pricing, OAuth/cloud-account auth, removal of internal context estimation, JSONL envelope migration.
- **checks / evidence:** Strict OpenSpec validation passed at proposal and after implementation. Baseline full `flutter test`: 449 pass (`/tmp/domovoy-baseline-tests.log`); source-frozen full `flutter test --reporter expanded`: 461 pass (`/tmp/domovoy-post-switch-full-tests.log`), including two new production-stack model-switch regressions and updated desktop goldens. `dart format .`: exit 0, 167 files/0 changed. `flutter analyze`: exit 0, no issues (`/tmp/domovoy-final-analyze-exact.log`). `flutter build linux --debug`: exit 0 (`/tmp/domovoy-post-switch-linux.log`). Normal web and Day 8 web both built successfully after the last source edit (`/tmp/domovoy-normal-web-switch-fix.log`, `/tmp/domovoy-post-switch-day08-web.log`). A normal web build outside the shared Flutter `build/` tree produced a complete asset manifest and Material Icons font (`/tmp/domovoy-normal-web-frozen.log`); the root verified icons and restored Claude selection through its stable relay copy. Root ran `tool/live_deepseek_test.dart` through the real production stack: Flash 41/17/58, restored 86/17/103, reasoning 146/29/175 with 27 reasoning, Pro 133/3/136; JSONL restore and ledger attribution matched raw SSE (`/tmp/domovoy-live-runtime.log`). Manual normal browser verified save/reload, provider selection, key isolation, Flash 59/138/197 and Pro 127/3/130. After a model-switch correction, the root rechecked fresh Flash→Claude and restored Pro-with-history→Claude in the browser: both switched and retained history; native reasoning shows disabled “По умолчанию”. Day 8 browser verified short 25/4/29 and a final long run of 34/17/51, 666/24/690, 707/14/721, with exact history 1407/55/1462 and last cache read 512; one overflow HTTP 400 exposed the provider message with no usage or retry. Separate live cache-hit evidence is `/tmp/domovoy-evidence/cache-observations.json`.
- **formal commands / exits:** `dart format .` → 0; `flutter analyze` → 0; `flutter test --reporter expanded` → 0; `flutter build linux --debug` → 0; `flutter build web --no-web-resources-cdn --target lib/live_web_main.dart --output build/web` → 0; `flutter build web --no-web-resources-cdn --target lib/day08_web_main.dart --output build/day08-web` → 0; `openspec validate add-provider-discovery-and-api-usage --strict` → 0; `.opencode/bin/repo-git diff --check` → 0. The two `build/` output directories lost shared font assets when built sequentially; the separate `/tmp/domovoy-normal-web-frozen` output contained `assets/FontManifest.json` and `assets/fonts/MaterialIcons-Regular.otf` and passed browser icon verification. The versioned change is source-frozen; stable Day 8 artifact copying may continue without changing this snapshot.
- **V1 verifier rework:** The first independent verdict found one defect: an offline restart discarded cached new model IDs for providers without a native model-list endpoint. `ProviderModelCatalog.refresh` now retains their last-good cached IDs only when remote metadata failed, while a successful validated metadata generation removes IDs no longer listed. A cached new Perplexity ID proves both transitions across restart (`test/infrastructure/llm/provider_discovery_test.dart`). Focused discovery tests → 6/6 (`/tmp/domovoy-v1-discovery.log`); `dart format .` → 0/0 changed; `flutter analyze` → 0/no issues (`/tmp/domovoy-v1-analyze.log`); final full `flutter test --reporter expanded` → 0/462 pass (`/tmp/domovoy-v1-full-tests.log`); `flutter build linux --debug` → 0 (`/tmp/domovoy-v1-linux.log`); normal web build to `/tmp/domovoy-v1-normal-web` → 0 with font manifest and Material Icons verified (`/tmp/domovoy-v1-normal-web.log`); `openspec validate add-provider-discovery-and-api-usage --strict` → 0; `.opencode/bin/repo-git diff --check` → 0. No paid DeepSeek request or manual Day 8 run was repeated because V1 changes only catalog reconciliation. `tasks.md` now marks 18/18 implemented items with family-level, targeted, and manual evidence rather than claiming per-profile live fixtures or a complete fuzz matrix.
- **frozen changed-tree fingerprint:** `worktree-sha256:ef6b548825d04a23bc96c3c3a7fcbc10b72479d0224773267ec5c1a37e0a250f` over 60 sorted changed or untracked versionable files, hashing path, byte length, and contents separated by NUL; excludes only this self-referential feature-state file. Previous frozen main snapshot before V1: `worktree-sha256:8b9bb1256c0e034e8cde3c7fde286cb4cb8aaa753b6d9089344c95c78eb3aaf3`.
- **open_findings:** Native Anthropic/Gemini adapters expose text and reported usage only; tools and explicit thinking controls require continuation signatures/verified request mapping and are not advertised. Ant Ling has no live list/metadata feed and uses Pi's verified default `Ring-2.6-1T` with unknown bounds. Runtime model metadata source is models.dev, with a bundled Pi-aligned snapshot; documented provider lists take priority.
- **rework_count:** `1`

- **independent_verification:** `PASS`, fix cycle 1. The independent replacement verifier confirmed the 60-file fingerprint, closed V1 using the offline/recovery regression, and retained all previous acceptance-criterion PASS verdicts. No blocking findings remain. Verification did not claim the unavailable DeepSeek verifier model.
