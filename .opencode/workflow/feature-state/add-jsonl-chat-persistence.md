# Feature State — add-jsonl-chat-persistence

- **change:** `add-jsonl-chat-persistence`
- **result:** Accepted durable JSONL persistence: production creates one lazy conditional native/browser store, injects and exposes its repository/catalog ports, preserves transient prompts, and passes independent AC-01–AC-11 verification.
- **scope_id:** `jsonl-chat-persistence-production`
- **attempt_id:** `implementation-1`
- **stage:** `accepted`
- **task_ids / AC:** tasks `1.1–6.2` complete; overall progress `19/19`. Final independent verifier passed `AC-01–AC-11` and all formal criteria.
- **initial_tier:** `T2`
- **current_tier:** `T2`
- **risk evidence:** Production persistence selection, dependency exposure, durable restart/deletion, no-fallback failures, and transient-vs-repository session lifecycle are T2 contracts.
- **included scope:** `ProductionAgentStack`/`DomovoyDependencies` repository/catalog injection and exposure; default conditional JSONL composition; fresh production-stack filesystem restart/delete tests; transient prompt no-storage test; injected backend failure test; complete writer-owned formal check matrix.
- **excluded scope:** Independent verification task `6.2`; archive/commit; token-accounting expansion; chat UI; filesystem/browser details in core or UI; new migrations; cross-process/cross-tab writers; secure erase.
- **changed paths in H4:** `lib/app.dart`; `test/app_composition_test.dart`; `test/widget_test.dart`; assigned checkboxes in `openspec/changes/add-jsonl-chat-persistence/tasks.md`; this state file.
- **base_revision:** `8d0828dfe845b89b8a0e80fccf38920f848eee70`
- **contract_revision:** Current proposal/design/specs were reread and not edited. Implementation follows the accepted conditional JSONL composition and transient prompt decisions.
- **starting_dirty_diff:** H4 started from accepted H1–H3 plus their process evidence. H1 verifier `ses_f68885436ffeD1kcnyS7ZibaC0`, H2 verifier `ses_f6872a6bdffe3smTQQrTldICwo`, and H3 verifier `ses_f685a34a1ffeO1Dn7GpOsiaDRf` returned PASS/CHECKS_PASS. Exact accepted H3 snapshot: `worktree-sha256:ec98332a988ff5e6bafdae066c943efebdaf012e8b3ab7ecb45c89f6d48662eb`; no staged diff.
- **recommended_mode:** `heavy`
- **execution_mode:** `heavy`
- **rationale:** The final slice joins production composition with accepted native/browser persistence and verifies exact restart, delete, and no-fallback behavior end to end.
- **selection_source:** User selected Heavy and assigned H4 through all writer-owned tasks.
- **model_tiers_used:** H4 implementation: strong Sol (`openai/gpt-5.6-sol`); required independent verifier: DeepSeek. No general Sol review is required.
- **role task_ids:** writer `jsonl-chat-persistence-production` / `implementation-1`; accepted H1 verifier `ses_f68885436ffeD1kcnyS7ZibaC0`; accepted H2 verifier `ses_f6872a6bdffe3smTQQrTldICwo`; accepted H3 verifier `ses_f685a34a1ffeO1Dn7GpOsiaDRf`; final verifier `ses_f684dbbceffesOY8EiQ5eCrj41`.
- **gates:** No user/product approval gate. Heavy acceptance is complete and archival is authorized.
- **composition behavior:** `buildProductionAgentStack` creates one `JsonlAgentSessionStore(createPlatformJsonlStreamStorage())` when no replacement pair is supplied. The same object is exposed as `AgentSessionRepository` and `AgentSessionCatalog` and injected into `InMemoryAgentRuntime`; paired replacement ports retain exact identity. `DomovoyDependencies.production` exposes those same ports. Supplying only one replacement port is rejected rather than mixing stores.
- **restart/delete evidence:** A temporary application-support namespace is written through a first production stack with two complete records. Fresh second/third stacks over new filesystem backend/store instances list and load exact messages, usage, continuation, compaction and summary metadata; runtime restoration matches the record; close acknowledgement advances revision durably; expected-revision deletion survives another reconstruction; the tombstoned id remains absent/reserved and the survivor remains exact.
- **transient/no-fallback evidence:** Two existing production prompt one-shot calls retain distinct transient IDs and independent HTTP requests while the injected durable catalog and filesystem key list remain empty. Injected list/read backend failures are exposed from the exact repository/catalog/runtime object as sanitized `AgentErrorKind.persistence`; no `InMemoryAgentSessionRepository` production fallback remains.
- **format / focused evidence:** cwd `/home/auroraos/omp/personal/domovoy`; `dart format .` → exit 0, `Formatted 106 files (0 changed)`; focused app/core/JSONL/filesystem/import tests → exit 0, `+128: All tests passed!`; `flutter test --platform chrome test/infrastructure/agents/jsonl/jsonl_browser_storage_test.dart` → exit 0, `+7: All tests passed!`.
- **full check evidence:** `flutter analyze` → exit 0, `No issues found!`; full `flutter test` → exit 0, `+327: All tests passed!` (log `/home/auroraos/.local/share/opencode/tool-output/tool_097af4bb1001xoxgGFU0ub8lo6`).
- **platform evidence:** exact `flutter build web --debug` → exit 0, wasm dry run succeeded, `Compiling lib/main.dart for the Web... 31.4s`, `✓ Built build/web`; exact `flutter build linux --debug` → exit 0, `✓ Built build/linux/x64/debug/bundle/domovoy`.
- **formal evidence:** `openspec validate add-jsonl-chat-persistence --strict` → exit 0, valid; all `19/19` tasks complete after final independent verification; `.opencode/bin/repo-git diff --check` → exit 0/no output; static checks found no in-memory production fallback or storage-platform details in feature/UI sources and confirmed the conditional factory plus exposed narrow ports.
- **spec sync / archive:** Delta `agent-runtime` fully replaced the accepted persistence-seam requirement while preserving unrelated main-spec content; new `agent-session-persistence` was added to main specs with all five accepted requirements. Exact post-sync comparison matched both deltas. The complete change, including `tasks.md` with `19/19` checked, is archived at `openspec/changes/archive/2026-09-13-add-jsonl-chat-persistence/`; `openspec validate --specs --strict` passes all 8 main specs and `openspec list --json` reports no active changes.
- **H4 implementation fingerprint:** SHA-256 manifest over `lib/app.dart`, H4 integration/widget tests, and `tasks.md`, excluding this self-referential process file: `3413597842462951dafac09bae55451c652fc10e62f1395b71b557afcc2e6ed7`.
- **dependencies / environment:** Flutter `3.41.4`, Dart `3.11.1`, Chrome browser test target, Linux x64 host; direct runtime `path 1.9.1`, `path_provider 2.1.6`, `shared_preferences 2.5.5`; direct test-only `shared_preferences_web 2.4.3`; resolver reports 14 newer incompatible versions without failure.
- **ordinary correction:** The first focused integration run expected the pre-restore revision after closing a restored repository session; runtime close correctly durably checkpointed revision 1. The test was corrected to verify that acknowledged successor and its exact content across the third stack; the rerun and all final checks passed.
- **final acceptance:** verifier `ses_f684dbbceffesOY8EiQ5eCrj41` returned PASS/CHECKS_PASS for `AC-01–AC-11` at exact frozen implementation `worktree-sha256:e94018976755ee33314be7ad94c60f940cd6777baa75901b72fb78af917292e5`, result ID `check-bb18d87c50429d0ee937d3ab5f365577f6a27b0dce0d6ef2a1664a3860ab1ee8`; no blockers or findings remained.
- **open_findings:** none.
- **deviations / blockers:** No contract deviation or remaining blocker. Same-process coordination remains per store instance as designed; multi-process/cross-instance writers remain unsupported.
- **rework_count:** `1`
