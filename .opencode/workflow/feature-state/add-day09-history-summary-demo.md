# Feature State — add-day09-history-summary-demo

- **change / scope_id:** `add-day09-history-summary-demo` / `day09-branch-demo`
- **attempt_id / stage:** `implementation-1` / `accepted`
- **result:** Day 9 branch adds a real 14-step comparison between an uncompacted production agent session and a production agent session using structured summary every 10 completed raw user/assistant messages. The summary retains the last two complete pairs and is persisted with its cadence provenance. The page compares real answers and API ledger usage, charging summary generation separately.
- **base_revision / branch:** `86c558c07d722a4ed8cbe0d5173601b6c722c84b` / `codex/day-09`; starting tracked diff was empty in this dedicated worktree.
- **contract_revision:** `planning-1`, spec-driven proposal/design/spec/tasks validated strictly. User's full implementation instruction authorizes this branch's work; no additional approval gate applies.
- **initial_tier / current_tier / mode:** `T2 / T2 / heavy`, because runtime cadence, JSONL provenance, billing ledger, two-session state, and browser UI interact. Parent orchestrator assigned one Sol writer; no second writer changed the branch.
- **expected_changed_paths:** `lib/app.dart`, `lib/core/agents/clock.dart`, `lib/core/agents/summary_compactor.dart`, `lib/demos/day09_*.dart`, `lib/day09_*.dart`, `assets/day09_scenario.json`, `pubspec.yaml`, `test/day09_demo_test.dart`, `docs/day09-demo.md`, this state file and branch-local OpenSpec artifacts.
- **excluded scope:** Changes to normal production compaction policy, pricing estimates, main branch, Day 10, commit/push before parent review.
- **implementation detail:** Production-built runtimes now use fresh secure per-runtime ID namespaces, while direct `AgentIdFactory()` retains its old IDs. The Day 9 restart test reproduced a legacy `message-1` collision at `AgentTranscript.append` before this opt-in production composition fix, then proved restore-and-append works. Separate dynamic Day 9 pair IDs and a saved active pointer prevent reset from colliding with old JSONL records.
- **checks / evidence:** Final 14-step focused Day 9 tests passed 6/6 (`/tmp/domovoy-day09-14-focused.log`): cadence at pairs 5 and 10, retained four raw messages, separate summary/assistant ledger totals, JSONL restart and step 11, all 14 identical prompts, reset, legacy-ID restoration, and explicit reset after a saved scenario changes. Core summary-compactor tests also passed with the Day 9 focused tests (`/tmp/domovoy-day09-focused-v3.log`). Final `flutter test --reporter expanded` passed 468 tests (`/tmp/domovoy-day09-14-full.log`). `flutter analyze` found no issues (`/tmp/domovoy-day09-14-analyze.log`). `dart format .` found 175 files, 0 changes (`/tmp/domovoy-day09-14-format-exact.log`). `flutter build linux --debug` passed (`/tmp/domovoy-day09-14-linux.log`). Strict OpenSpec validation and repo diff check passed. Final Day 9 web release built from `lib/day09_web_main.dart` to `/tmp/domovoy-day09-web-v5` (`/tmp/domovoy-day09-web-build-v5.log`), with `assets/FontManifest.json` and `assets/fonts/MaterialIcons-Regular.otf` present; stable served copy is `/tmp/domovoy-day09-web-served-v5`.
- **manual feedback before independent review:** First full real DeepSeek browser run with the generic summary instruction: baseline 17357 input / 932 output / 18289 total; summarized answers 15383 / 958 / 16341, two summary calls 2816 / 1574 / 4390, combined 18199 / 2532 / 20731 (2442 more than baseline). The final answer retained 7/8 control facts, omitting project name; latest request input was 1928 versus baseline 2709. A second run with only a terse user-level instruction still generated 515 output tokens for its first summary and reached the 768-token output cap at the second, so the UI stopped at step 10 with a compaction failure. This led to the opt-in system-level continuity checklist, a 1536-token safety cap, and a compaction-specific error message. Direct real DeepSeek probes (`/tmp/domovoy-evidence/day09-summary-probe*.json`) then returned valid schema JSON: first representative summary 358 output tokens; final second summary 445 output tokens and explicitly retained the eight source values available by step 8, including exact project name and slot duration. A third full UI run remains pending; no quality or net-savings claim is made from probes alone.
- **third manual browser run (12 prompts):** With the final summary instruction, both modes returned all eight control facts, both summary generations completed, and JSONL reload retained progress and API usage. Baseline 16921 input / 861 output / 17782 total, cache read 14208; summary answers 13760 / 1193 / 14953, cache read 9344; two summary calls 3586 / 619 / 4205, cache read 384; combined 17346 / 1812 / 19158, cache read 9728. Last answer input fell from 2601 to 1263 (about 51% smaller), while total spend was 1376 higher due to summary overhead. Two acceptance-check turns with no new decisions were then inserted before the final prompt, yielding a 14-step shared scenario for Day 9 and Day 10. The new full comparison is pending; no net-savings claim is made yet.
- **final manual browser run (14 prompts):** The older 12-step pair was detected as incompatible, explicit reset created a fresh pair, and run-all completed 14 identical prompts in each production session. Both final answers contained all 8/8 control facts. Baseline 14 physical answers: 22906 input / 1570 output / 24476 total, cache read 19584 (85.5% hit). Summarized 14 answers: 18080 / 1983 / 20063, cache read 13184; two separate summary calls: 3380 / 793 / 4173, cache read 512; combined 16 calls: 21460 / 2776 / 24236, cache read 13696. Actual net reduction was 240 tokens (0.98% of baseline), while final answer input fell from 3692 to 2710 (26.6%). Compaction provenance showed two generations, 20 processed raw messages, and four verbatim retained messages. A final page reload restored all 14 steps, answers, summaries, and the same reported API totals (`/tmp/domovoy-evidence/day09-final-live.json`). No extrapolated saving is claimed.
- **frozen changed-tree fingerprint:** `worktree-sha256:4f2478521e7f43c4e196673f94cc157a11d1ea05d69fe0c1167e1e21dc92d42f` over 19 sorted changed or untracked versionable files, hashing path, byte length, and contents separated by NUL; excludes only this self-referential feature-state file.
- **open_findings:** The summary's physical overhead may exceed assistant-input savings over 12 prompts; UI presents both without claiming a net saving. If one of the two modes fails midway, already acknowledged ledger entries remain and the UI requires an explicit reset rather than silently replaying a mode.
- **rework_count:** `0`
- **independent_verification:** Pending frozen branch review.

- **accepted:** Independent replacement verifier `PASS` for day09-branch-demo. Confirmed frozen 19-file fingerprint `4f2478521e7f43c4e196673f94cc157a11d1ea05d69fe0c1167e1e21dc92d42f`, all acceptance criteria, and 468-test log. No blocking findings; no DeepSeek verifier model claimed. Root final browser reload restored 14/14 and exact recorded API totals.
