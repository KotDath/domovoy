---
name: team-orchestration
description: "Координируйте сессии architect, coder и reviewer в репозитории. Используйте, когда роли нужно поставить задачу, задать межролевой вопрос или ответить на него, отправить решение или finding, сообщить о завершении или блокировке, дождаться другой роли либо связаться через Herdr. Сохраняйте промпты ролей независимыми от транспорта, даже когда упоминается Herdr или Orca."
---

# Оркестрация команды

Используйте семантические командные операции, оставляя детали транспорта внутри
этого скилла. Межролевые сообщения и независимые primary-сессии идут через
Herdr; в будущем этот транспорт можно заменить реализацией на Orca без изменения
промптов агентов. Ограниченное read-only исследование выполняет штатный tool
`task` с built-in subagent `explore`, а не отдельная сессия Herdr или Orca.

## Роли и маршруты

Используйте стабильные живые имена агентов `architect`, `coder` и `reviewer`.

- `architect` ставит задачи `coder` и `reviewer`, а также отправляет записанные
  решения затронутым ролям.
- `coder` отправляет вопросы, результаты и блокировки роли `architect`.
- `reviewer` отправляет findings реализации роли `coder`, а блокировки в
  спецификации или архитектуре — роли `architect`.
- Избегайте циклических ожиданий. Споры о владении и нерешённые вопросы
  направляйте роли `architect`.

## Механизмы

- **Read-only исследование:** архитектор запускает штатный `task` с subagent
  `explore`. Не создавайте сессии Herdr или Orca только для explore, если
  `task` доступен и пользователь не запросил иной транспорт.
- **Independent primary roles и межролевые сообщения:** `architect`, `coder` и
  `reviewer` работают в отдельных сессиях; постановка задач, вопросы, решения,
  findings, результаты, ожидание и блокировки идут через транспорт Herdr.

## Привязка транспортного имени к роли

Herdr name и OpenCode agent — независимые параметры:

- `herdr agent start coder ...` задаёт адрес процесса в Herdr;
- `opencode --agent coder` выбирает инструкции и permissions роли.

Для OpenCode запускайте роль так:

```text
herdr agent start <role> \
  --kind opencode \
  --pane <pane-id> \
  -- --agent <role>
```

Не считайте роль выбранной, пока ответ `agent start` не подтверждает одновременно
Herdr name и аргументы OpenCode. До отправки TASK проверяйте, что `name == <role>`
и `argv` содержит `--agent <role>`.

## Контракт explore-задания

Каждое задание `explore` должно содержать:

- границы исследования и допустимые пути;
- вопросы и ожидаемый формат ответа;
- какие доказательства нужно вернуть (файлы, строки, наблюдаемые факты);
- явный запрет правок и state-changing команд.

Отчёт subagent — это черновые наблюдения, а не решение и не итоговое ревью.
Архитектор перепроверяет выводы до записи решений или постановки задач
`coder`/`reviewer`. Subagent `explore` не реализует приложение и тесты, не
меняет OpenSpec и не заменяет primary-сессии `coder` и `reviewer`.

## Контракт сообщения

Отправляйте полные самодостаточные сообщения:

```text
Тип: TASK | QUESTION | DECISION | FINDING | RESULT | BLOCKED
Изменение: <OpenSpec change, задача или область настройки инструментов>
От: <роль>
Кому: <роль>
Контекст: <краткое место и текущее состояние>
Доказательства: <файлы, строки, команды или наблюдаемые результаты>
Сообщение: <запрос, ответ, finding или результат>
Ожидаемый ответ: <конкретный ответ или "не требуется">
```

Существенные решения, влияющие на scope, требования, design или tasks, не
считаются зафиксированными, пока `architect` не запишет их в OpenSpec и не
уведомит затронутые роли.

## Транспорт Herdr

Перед использованием Herdr загрузите скилл `herdr` и следуйте его актуальным для
установленной версии инструкциям по CLI, проверке среды, поиску цели, ожиданию,
чтению и безопасности. Не угадывайте команды и не обходите проверку `HERDR_ENV`.

Для каждой операции:

1. Убедитесь, что отправитель работает в pane под управлением Herdr.
2. Проверьте живых агентов и найдите цель по уникальному имени роли.
3. До доставки сообщения проверьте состояние цели.
4. Отправьте структурированное сообщение через интерфейс агентов Herdr.
5. Подтверждайте только то, о чём действительно сообщил Herdr.

Используйте следующую семантику:

- **Постановка задачи:** доставьте `TASK`, не дожидаясь завершения всей работы.
  Отправитель сохраняет ответственность за координацию и может позднее дождаться
  результата или проверить состояние.
- **Вопрос:** доставьте `QUESTION`, дождитесь стабильного состояния цели,
  прочитайте ответ и при необходимости оформите его как `DECISION`.
- **Отчёт:** доставьте `FINDING`, `RESULT` или `BLOCKED`; ждите подтверждения
  только тогда, когда от него зависит дальнейшая работа.
- **Ожидание:** используйте ожидание жизненного цикла агента в Herdr, затем
  проверьте состояния blocked или unknown, прежде чем считать работу завершённой.

## Обратный маршрут

- При синхронном `agent prompt --wait` получатель возвращает структурированный
  ответ в текущей сессии; отправитель читает его через Herdr. Повторный вызов
  Herdr получателем не требуется.
- Для асинхронного или инициативного сообщения получатель адресует его живому
  агенту `architect` через Herdr.
- Использование Herdr CLI из уже запущенной primary-сессии для отправки
  межролевого сообщения не считается созданием дополнительной сессии.
- Не запрещайте роли пользоваться Herdr целиком. Если доставка дублируется,
  уточните маршрут конкретно, не используйте формулировку «не запускай Herdr».

Если Herdr недоступен, целевая роль отсутствует, доставка зависла или цель
перешла в состояние blocked, сформируйте локальное сообщение `BLOCKED` с
наблюдаемыми доказательствами. Никогда не утверждайте, что доставка успешна,
только на основании попытки отправить ввод в терминал.
